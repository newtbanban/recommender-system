# SAR(Simple Algorithm for Recommendation)
## concept

# ALS(Alternating Least Squares)
## concept
So basically, ALS is used for dealing with big data. By using **Spark**, we can distribute the data to different computers (nodes) **or different CPU cores on a single machine**, allowing us to work in parallel.